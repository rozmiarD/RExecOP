# First-Run Demo Inspect

This fixture runbook is intentionally local-only. It is used to prove that
RExecOp can load a profile, validate environment and catalog inputs, explain an
operation, and create a dry-run plan without touching external infrastructure.
